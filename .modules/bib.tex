\usepackage[authordate, backend=biber]{biblatex-chicago}

% -----------------------------------------------------------------------------

% Add the following to the preamble to load the `.bib` file:
% \addbibresource{file.bib}

% Use the following to print the bibliography:
% \printbibliography[heading=bibintoc]{}

% But use the following in a Beamer presentation:
% \begin{frame}[allowframebreaks]
%     \printbibliography[heading=bibintoc]{}
% \end{frame}
