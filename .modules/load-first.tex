% Load these before font packages
\usepackage{amsmath}
\usepackage{amsthm}
\usepackage{mathtools}
\mathtoolsset{
    showonlyrefs=true,
    showmanualtags=true,
}

% Allow loading packages conditional on the engine
\usepackage{iftex}

% Package `xcolor` will be loaded by other packages. Load it here to make sure
% the correct options are passed.
\usepackage[dvipsnames, table]{xcolor}
