\usepackage{zref-clever}

\newcommand{\cref}[1]{\zcref[nameinlink=false, cap]{#1}}
\newcommand{\Cref}[1]{\zcref[nameinlink=false, cap]{#1}}

% Package `beamer` redefines `\label`, which might cause compatibility issues.
% Consult the manual of `zref-clever` for workarounds.

% The manual recommends that `hyperref` be loaded after all other packages.
