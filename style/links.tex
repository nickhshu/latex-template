\usepackage[]{hyperref}
\hypersetup{
    allcolors = violet,
    breaklinks = true,
    colorlinks = true,
    citecolor = black, % no reason to distract the reader
}

% Load after `hyperref` and `amsmath` but before proclamation environments
\usepackage[capitalize,noabbrev]{cleveref}
