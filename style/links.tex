\usepackage[]{hyperref}
\hypersetup{
    allcolors = violet,
    breaklinks = true,
    colorlinks = true,
    hypertexnames = false, % Otherwise `perpage` will lead to duplicate anchors
}

% Package `cleveref`
%
% Load after `hyperref` and `amsmath` but before proclamation environments
% Do not load with `beamer`, which is not compatible with it.
\makeatletter
\@ifclassloaded{beamer}{}{
    \usepackage[capitalize,noabbrev]{cleveref}
}
\makeatother
