% Load these before font packages.
\usepackage{amsmath}
\usepackage{amsthm}
\usepackage{mathtools}

% Loaded here since it is used by many packages. However, in theory one should
% load it after `lwarp`.
\usepackage[
    dvipsnames,
    table, % necessary for `sgamex`
]{xcolor}
